import React from 'react';

import { Container, Title, ExpandIcon } from './styles';

const ServerName: React.FC = () => {
    return (
        <Container>
            <Title>TypeScript & ReactJS</Title>
            <ExpandIcon />
        </Container>
    );
};

export default ServerName;
